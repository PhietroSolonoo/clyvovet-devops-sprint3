# Cronograma de Desenvolvimento – ClyvoVet

**Responsável pela implementação:** Phietro Solon Oliveira (RM563842)  
**Colaboradores:** Matheus Almeida Ribeiro (RM562980), Gustavo Barrios de Araujo (RM563358)

---

| Data       | Módulo / Atividade                           | Descrição das tarefas executadas                                                                                                                                                                                                                                                                                                   |
|------------|----------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **19/05**  | Infraestrutura e configuração                | Criação da estrutura de pacotes, arquivos Gradle (`build.gradle`, `settings.gradle`), `.gitignore`, `application.properties` (conexão com Oracle), classe principal `ClyvovetApplication`. Inicialização do repositório Git e commits iniciais.                                                                                  |
| **20/05**  | Modelagem de dados                           | Implementação dos 6 enums (`Especie`, `Sexo`, `TipoConsulta`, `StatusConsulta`, `StatusMedicamento`, `TipoAlerta`) e das 10 entidades JPA (`Endereco`, `Tutor`, `Pet`, `Clinica`, `Veterinario`, `Consulta`, `Vacina`, `Medicamento`, `Exame`, `AlertaSaude`), incluindo relacionamentos (`@ManyToOne`, `@OneToMany`) e sequences Oracle. |
| **21/05**  | Camada de persistência e DTOs                | Criação de 9 interfaces `Repository` com JPQL e Query Methods (consultas paginadas, filtros). Desenvolvimento de 9 DTOs de request (Bean Validation) e 9 DTOs de response. Criação de 9 mappers (`@Component`) com métodos `toEntity` e `toResponse`.                                                                             |
| **22/05**  | Regras de negócio e cache                    | Implementação de 9 classes `Service` com lógica de negócio, anotações de cache (`@Cacheable`, `@CacheEvict`), métodos de busca com filtros e paginação. Adição de métodos auxiliares (ex: `marcarComoLido` em `AlertaSaudeService`).                                                                                              |
| **23/05**  | Camada de apresentação e tratamento de erros | Criação de 9 controllers REST (`@RestController`, `@RequestMapping`, `@Valid`, `Pageable`, códigos HTTP 201/200/204/404/400). Configuração do Swagger UI (`springdoc-openapi`). Implementação do `GlobalExceptionHandler` (captura 404, 400, 500) e `ResourceNotFoundException`.                                                 |
| **24/05**  | Testes, ajustes e documentação               | Configuração da conexão com Oracle (`oracle.fiap.com.br:1521:orcl`), testes de CRUD via Postman, ajustes nos repositórios (métodos paginados). Geração do DER (Oracle SQL Developer), diagrama de classes UML (IntelliJ), coleção Postman (`ClyvoVet API.postman_collection.json`), README.md e cronograma.                     |